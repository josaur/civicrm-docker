apiVersion: v1
kind: Service
metadata:
  name: {{ include "civicrm-drupal.fullname" . }}
  labels:
    {{- include "civicrm-drupal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "civicrm-drupal.selectorLabels" . | nindent 4 }}
